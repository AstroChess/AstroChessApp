export const environment = {
  production: true,
  server: {
    url: 'https://localhost:3000',
    loginEndpoint: {
      endpoint: '/login'
    },
    signupEndpoint: {
      endpoint: '/register'
    }
  }
};